version: '3.8'

services:
  seo-content-graph-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: seo_graph_agent
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
